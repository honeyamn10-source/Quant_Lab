services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./datasets:/workspace/datasets
      - ./experiments:/workspace/experiments
    command: uvicorn quantlab.api.main:app --host 0.0.0.0 --port 8000 --reload